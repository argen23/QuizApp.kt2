<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.quizappkt2">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".kolg.App"
        android:allowBackup="true"
        android:icon="@drawable/artut"
        android:label="@string/app_name"
        android:roundIcon="@drawable/artut"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuizAppkt2">

        <activity android:name=".kolg.uitls.SplashScreenActivity"
            android:theme = "@style/Theme.SplashScreenTheme"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity
            android:name=".kolg.presentation.MainActivity"
            android:exported="true"
            android:label="@string/app_name">


        </activity>
    </application>

</manifest>